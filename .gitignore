# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Accidental artifacts (do not commit binaries / extension drops)
ziywAS4P
*.zip
extension-backups/
wdiwf-extension-v4.1.0 2/

# Supabase CLI local cache (do not commit)
supabase/.temp/

# Local Supabase deploy / health-check secrets (see scripts/supabase/env.supabase.local.example)
.env.supabase.local

# Optional: ignore timestamped content-engine runs (uncomment if repo gets noisy)
# newsletter/outputs/live/run-*
# newsletter/outputs/friday/run-*
.vercel

# Temp build artifacts
.tmp/
